#include <stdio.h>

struct A{
  char a;
  int b;
};

struct B{
  char a;
  int b;
}__atribute__((packed));

int main(){
  char str[10] = {'A', 0, 0, 0, 'B', 0, 0, 0, 0};
  struct A * a;
  a = str;
  printf("%c %c", a->a, (*(a + 1)).a);
  return 0;
}
